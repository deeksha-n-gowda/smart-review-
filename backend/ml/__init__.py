"""
ml/ — Machine Learning analysis pipeline.

Modules:
    analyzer.py   — Static analysis engine combining rule-based checks + mock ML scoring.
    explainer.py  — SHAP/LIME explanation generator for vulnerability findings.
    rules/        — Per-language rule definitions (Python, Java, C#, JavaScript).
"""
