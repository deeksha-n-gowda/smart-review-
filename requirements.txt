# =============================================================================
# AI Code Review Assistant — Python Dependencies
# Install with: pip install -r requirements.txt
# =============================================================================

# --- Web Framework ---
Django==4.2.13
djangorestframework==3.15.1
django-cors-headers==4.3.1          # Allow frontend to call the API cross-origin
gunicorn==23.0.0                    # Production WSGI server (Docker image CMD)

# --- Database ---
psycopg2-binary==2.9.10              # PostgreSQL adapter (for production)
# SQLite is built into Python — used by default in dev

# --- Encryption ---
pycryptodome==3.21.0                # AES-256-GCM encryption for code snippets

# --- Code Analysis Helpers ---
# (SHAP/LIME explanations are generated by ml/explainer.py using a built-in
#  rule-weight model — no ML training libraries are required.)
pylint==3.1.0                       # Python AST/lint analysis
radon==6.0.1                        # Cyclomatic complexity metrics for Python
javalang==0.13.0                    # Java source code tokenizer/parser

# --- Utilities ---
python-dotenv==1.0.1                # Load .env files into os.environ
Pillow==11.0.0                      # Image handling (for report generation later)
requests==2.31.0                    # HTTP client (for inter-service calls)

# --- Dev / Testing ---
pytest==8.2.0
pytest-django==4.8.0
pytest-cov==5.0.0
factory-boy==3.3.0
coverage==7.5.1
